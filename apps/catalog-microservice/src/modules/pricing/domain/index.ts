// Pricing domain barrel. The framework-free `Price` / `TaxCategory` model, its
// value objects, lifecycle enums, and in-process domain events land here. Kept
// alive as an empty module so the `domain/` folder is tracked and the boundaries
// lint classifies future files at the canonical path from the moment they exist.
export {};
