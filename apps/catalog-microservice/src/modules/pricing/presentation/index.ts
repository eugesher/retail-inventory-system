// Pricing presentation barrel. The `PricingController` with its `@MessagePattern`
// RPC handlers lands here (the pricing microservice surface is RMQ-only, like
// catalog). Kept alive as an empty module so the `presentation/` folder is
// tracked.
export {};
