import { Module } from '@nestjs/common';

// Empty for now: the catalog bounded context grows its
// domain / application / infrastructure / presentation layers in later work.
@Module({})
export class CatalogModule {}
