# Simplification Carryover

This document is the **only shared state** across clean-context simplification phases. Each phase reads it on entry and updates it on exit. Treat all content here as load-bearing; do not delete entries from prior phases.

## How to use this document
- On phase entry: read every section in full.
- "Established patterns" is normative — apply listed conventions consistently in your phase.
- "Files modified" tells you what has already been touched; do not re-simplify out of scope.
- "Open / deferred" is a working triage list; consult it for items that may be in your phase's scope.
- On phase exit: update the relevant sections per your task file's Procedure step 5. Do not remove prior entries.

## Phase ledger
### Completed
- phase-01 — foundational-libs. Net diff: 10 files changed, 20 insertions / 52 deletions. The five libs were already extremely lean — most of the surface is plain types, enums, re-export barrels, and abstract base classes. The actionable findings clustered in three areas: (1) stale task-ID narrative comments in `libs/common/index.ts`, `libs/contracts/microservices/microservice-message-pattern.enum.ts`, `libs/contracts/retail/events/order-cancelled.event.ts`, and `libs/contracts/inventory/inventory.constants.ts` (removed/trimmed, preserving the WHY portions about dual-registry intent and cross-service threshold sharing); (2) `libs/ddd` base classes carrying introductory narration that the type signatures already convey (entity / aggregate-root / value-object / domain-event / repository-port headers trimmed to WHY-only); (3) one truly-unused public getter — `AggregateRoot.domainEvents` (only the spec read it, no production caller) — was deleted, and the two spec tests covering it were merged into a single "buffers added events and drains them on pull" test. Also collapsed the redundant `if (other === undefined || other === null)` pair in `Entity#equals` / `ValueObject#equals` to `if (other == null)`. `libs/config` (Joi schema) and `libs/database` (BaseEntity, BaseTypeormRepository, DatabaseModule, SnakeNamingStrategy re-export) had no changes — the Hard Constraint freezing their public surface ruled out the agents' "dead methods" suggestions, and the schemas were already minimal. No exported name in `libs/contracts` was renamed.

- phase-02 — infrastructure-libs. Net diff: 11 files changed, 24 insertions / 56 deletions. The four port/adapter libs land between phase-01's almost-touchless surface and the heavier app code that follows — they are mostly small Nest module wrappers, a single substantial adapter (`RedisCacheAdapter`), one substantial config class (`LoggerModuleConfig`), and a side-effect tracer bootstrap. Findings clustered in three areas: (1) **task-ID / migration-history narrative** in comments that paraphrased what the code now does (`routing-keys.constants` "Replaces the underscored …", `microservice-client-notification.module` "task-09", `cacheable.decorator` "task-04 / task-11", `redis-cache.adapter` "task-11 / ADR-016 migration", `cache-keys.CacheHelper` "Removed in task-14", `metrics.module` "Task-10", `trace-context.interceptor` "Task-10", `http-context.middleware` "preserved verbatim from the libs/common move") — all trimmed, leaving the live WHY intact. (2) **Class-narrating headers** that paraphrased the class name and decorators (`MessagingModule`, `RedisCacheAdapter`, `CacheModule`, `CacheHelper`) — trimmed to WHY-only, in line with phase-01. (3) **ADR-pointer lists** that decorated method intent (`cache.port.ts` listing ADR-002 / ADR-006 / ADR-016 / ADR-021 as a closing sentence) — trimmed; the contract intent stays but the pointer-tour is rot. `libs/auth` had zero diffs — the lib is genuinely small and every comment in it carries live WHY (the `@Public()` opt-out semantic, the validator-port keeping persistence out of libs/auth, the runtime re-export pattern of `RoleEnum`). Five surface items that grep proved had zero in-repo callers (`RabbitmqClientFactory`, `CacheHelper`, `@Cacheable` decorator, `MetricsModule`, `TraceContextInterceptor`, and the `ICorrelationPayload` re-export in `libs/observability/correlation.types.ts`) were left in place because CLAUDE.md documents them as part of each lib's exported surface; their narrative comments were still trimmed of rot.

- phase-03 — notification-microservice. Net diff: 8 files changed, 12 insertions / 27 deletions. As the canonical per-module template (ADR-011 §1), this is the first per-service phase and the shape established here is what phases 04–06 will mirror. The service is small (one bounded context, ~25 files) and already well-shaped, so the trims were almost entirely **class-narrating headers** and **WHAT-paraphrase comments**, with two carefully-preserved WHY snippets retained on the `EmailNotifierAdapter` / `WebhookNotifierAdapter` scaffolds (the "dependency deliberately not in package.json" decision is non-obvious to a reader who only sees the `throw new Error('not implemented')` body). Findings: (1) **Class headers paraphrasing the class signature** in `notification.model.ts` ("Notification is a value object — equality is by content..."), `log.notifier.adapter.ts` ("Default adapter — emits the notification as a structured Pino log line..."), and the two `test-doubles.ts` class headers — all deleted (the `extends ValueObject<...>`, `implements INotifierPort`, and `class X` shapes already convey the meaning). (2) **Hypothetical future-state postscripts** in `health.controller.ts` ("The gateway can proxy ... later") — deleted; the live RMQ-only WHY is what reviewers actually need. (3) **Module-level WHY tightened**, not removed: `notifications.module.ts` kept the DI-rebind seam description because ADR-011 §3's rejected alternative (NotifierService with feature flags) is non-obvious from the providers array; `notifier.port.ts` kept a single-line boundary WHY matching phase-02's `cache.port.ts` shape. (4) **Both placeholder adapter files preserved** with one-line ADR-anchored WHYs that name the deliberate-deferral decision. No code-path or test-case was removed. `main.ts` was not touched — the tracer-first import order is preserved. `notification-channel.enum.ts`, both barrel `index.ts` files, both consumer files (`OrderEventsConsumer`, `InventoryEventsConsumer`), and the use-case classes (`SendOrderNotificationUseCase`, `SendLowStockAlertUseCase`) had no comments at all and required no edits — phase-02's pattern of "comments earn their place" had clearly already been applied to the use-case layer during the original ADR-011 build.

- phase-04 — inventory-microservice. Net diff: 21 files changed, 80 insertions / 194 deletions. The largest per-service phase so far — inventory carries a heavier comment surface than notification because the stock module sits at the intersection of three architectural pressures (cache-aside per ADR-002/016/021/022/023, transactional persistence per ADR-017's opaque-port rule, and cross-service event publication for `inventory.stock.low`). Each of those left behind multi-paragraph ADR-tour blocks at the port/adapter seams. Findings clustered in five areas: (1) **Domain-layer class-narrating headers and hypothetical-future-state postscripts** were trimmed in all five files under `domain/` — `stock-item.model.ts` (kept the non-obvious "reservedQuantity defaults to 0 because the ledger doesn't track reservations separately" WHY, dropped the constructor-invariant restating and the "future ledger evolutions become invisible" postscript), `storage.model.ts` (full header deleted — `extends ValueObject<IStorageProps>` + the throwing constructor body convey every claim the comment made), the three event classes (kept the "fires when X" semantic contract per event, dropped the in-process-vs-cross-service narration about the publisher adapter). (2) **Port-file ADR-tour headers** (`stock-cache.port.ts`'s 14-line trailing paragraph that listed ADR-002 + ADR-021 + ADR-023) were trimmed to a single ADR-023 anchor that names the type-enforced post-commit ordering — matching the phase-02 precedent on `libs/cache/cache.port.ts`. The `IStockCacheGetResult.available` WHY (CACHE-005 closure) was preserved but tightened. The opaque-token WHY on `transaction.port.ts` was kept (the `unique symbol` brand is non-obvious to a future reader who would otherwise try to construct one inline). (3) **Audit-closure breadcrumb blocks on `stock.cache.ts`** (12-line "CACHE-001/004 closed by ADR-021... CACHE-003/009 closed by ADR-022... CACHE-005 closed by..." block) were compressed to a 4-line list by removing the per-closure mechanism narration — the audit-finding identifiers themselves are the load-bearing breadcrumb per the established pattern, and ADR-021/022/023 carry the mechanism details. (4) **Intentional-no-op method explanations** on `StockRabbitmqPublisher.publishStockReserved` and `StockTypeormRepository.findBySku` were trimmed to one-line "Intentional X, not Y" framings — these are method-level analogues of phase-03's placeholder-adapter pattern. (5) **Spec-file class-narrating headers** were trimmed in `application/use-cases/spec/test-doubles.ts` (three `In-memory X implementation. Stores Y by Z.` headers deleted, exactly matching the `InMemoryNotifier` / `FakeLogger` trim in phase-03). The canonical-reference header on `infrastructure/cache/spec/stock.cache.spec.ts` was deliberately preserved — DOCS-001 in the 2026-05-20 audit explicitly names it the canonical convention reference for inventory unit-test layout, so deleting it would invalidate the audit's `informational-no-action` verdict. The 13-line `AUDIT-2026-05-08 [CODE-001]` comment block above the `!!item.storageId` filter in `reserve-stock-for-order.use-case.ts` was preserved verbatim per the phase brief's hard constraint. The presentation layer (`stock.controller.ts`), the bootstrap (`main.ts`, `app.module.ts`, all `index.ts` barrels), all four TypeORM entity files (`Product`, `ProductStock`, `ProductStockAction`, `Storage`), `typeorm-transaction.adapter.ts`, `add-stock.use-case.spec.ts`, `typeorm-transaction.adapter.spec.ts`, and the two `domain/spec/` files had no narrative comments at baseline and required no edits — confirming that the "comments earn their place" rule had already been applied to the wire-format / decorator-driven layers during the original ADR-012 build, same as phase-03 found for the notification microservice's consumer layer.

- phase-05 — retail-microservice. Net diff: 23 files changed, 36 insertions / 169 deletions. Shape mirrors phase-04 closely — retail's `orders` bounded context carries the same three architectural pressures (cross-service confirm RPC via `INVENTORY_CONFIRM_GATEWAY`, cross-service event publication for `retail.order.created` / `retail.order.confirmed`, and per-RPC validator/loader pipes) and accumulated multi-paragraph ADR-tour blocks at the port/adapter/pipe seams. Findings clustered in six areas: (1) **Domain-layer class-narrating headers and hypothetical-future-state postscripts** were trimmed in all six files under `domain/`. `order.model.ts` had the heaviest surface — a 5-line `IOrderConfirmationResult` migration narrative ("replaces the legacy `OrderConfirmDomain` state-transition computer..."), a 14-line `Order` class header repeating the constructor-throw invariants and the ADR-013 §5 create-path-vs-aggregate asymmetry, plus 8-line method headers on `create` / `reconstitute` / `applyInventoryConfirmation` / `cancel`. Trimmed to keep three non-obvious WHYs: the per-quantity-expansion → `order_product` has no `quantity` column (otherwise the `for (i = 0; i < line.quantity; i++)` loop reads as a bug), the `confirmedOrderProductIds` semantic (subset for which stock was reserved, not the request set), and the `cancel()` "no producer today" breadcrumb so grep gives a positive. `order-product.model.ts` / `order-status.value-object.ts` lost their full class-narrating headers (the `extends Entity<number | null>` and the `static readonly PENDING/CONFIRMED` + `isPending`/`isConfirmed` shape encode them). `customer.model.ts` kept a one-line ADR-013 §3 anchor on the "Retail does not own a Customer aggregate" decision since the rejected-alternative is documented in the ADR. `order-product-status.value-object.ts` was comment-clean at baseline and required no edits. (2) **Event-class headers** trimmed in all three events — `order-created.event.ts` was reframed to name the ADR-013 §5 use-case-constructs-the-event invariant (the aggregate cannot fabricate its own id at create-time; the prior comment incorrectly suggested the event fired "after `Order.create` succeeds"). `order-confirmed.event.ts` kept the no-cross-service-subscriber breadcrumb. `order-cancelled.event.ts` dropped the `task-09 brief` task-ID rot, kept the "no producer wires this today" breadcrumb in the phase-04 `stock-released.event.ts` shape. (3) **Port-file ADR-tour headers and inline-method WHAT-paraphrase** were trimmed across the three port files — `inventory-confirm.gateway.port.ts` lost a 9-line class-narrating header and kept a one-line "reply is subset of reserved ids" WHY; `order-events.publisher.port.ts` dropped the `_carryover-07 §5 #3 / _carryover-08 §12 #5` carryover-ID pointer rot (exact phase-04 pattern from `stock-events.publisher.port.ts`) and kept the cold-Observable + broker-ack WHY; `order.repository.port.ts` lost its 13-line port-purpose header and **five inline method-purpose comments** that paraphrased the method signatures (`findConfirmableOrder`, `customerExists`, `findExistingProductIds`, the `OrderConfirmableProduct` alias narration), with the single non-obvious WHY (`findOrderResponse` returns joined reference-table fields verbatim because e2e snapshots assert on `name`/`color`) lifted to the file-level header so it doesn't decay across signature changes. (4) **Use-case ADR-lead-in blocks and migration narrative** trimmed in `create-order.use-case.ts` (5-line ADR-013 lead-in before `new OrderCreatedEvent(...)`, replaced with a one-line "publish failures never raise" invariant on the catch block) and `confirm-order.use-case.ts` (3-line "the existing legacy path did the same" migration-history rot on the inventory-RPC catch block, deleted entirely; 6-line drain-pattern + "publisher port surface is wired" hypothetical-future block before `pullDomainEvents()`, trimmed to a one-line invariant). The `get-order.use-case.ts` `IOrderHeaderView` 4-line interface header lost its hypothetical-future implication ("today the gateway only needs..."). (5) **Adapter and mapper headers** — `inventory-confirm.rabbitmq.adapter.ts` and `order-rabbitmq.publisher.ts` both kept their non-obvious WHYs but lost the carryover-ID pointer rot (`_carryover-08 §9 #4`, `_carryover-07 §5 #3`) in the exact phase-04 pattern. The cross-service contract-test claim ("TypeScript compile is the cross-service contract test", ADR-013 §7) was relocated from a method-body comment to a method header on `reserveOrderStock`, since the claim is non-obvious to a reader who would otherwise wonder why no runtime contract test exists. `customer.mapper.ts` lost its full class header (the class is a 3-line static-mapper; the VO discussion already lives on `customer.model.ts`). `order-typeorm.repository.ts` tightened the cross-service-`product`-table WHY (the load-bearing claim — the `product` table is owned by inventory but retail reads it for the order-create existence check — was preserved with an ADR-013 anchor). `orders.module.ts` was trimmed to the one-line `useExisting` rationale matching phase-04's `stock.module.ts` precedent. (6) **Presentation-layer pipe headers and spec headers** trimmed — both `order-create.pipe.ts` (5-line class-narrating header about presentation-vs-application + boundary-rule narration) and `order-confirm.pipe.ts` (4-line counterpart) lost their full headers; the `@Injectable()` + the `IOrderRepositoryPort` injection + the `RpcException` throws are self-documenting. `order.mapper.spec.ts`'s 4-line round-trip-purpose header was deleted (matches phase-04's "spec class-narrating headers are deleted" rule). `order.model.spec.ts`'s 6-line migration-history header ("Migrated from `app/api/order/domain/spec/order-confirm.domain.spec.ts`...") was deleted — the test file already enforces the assertion shape against the new return type, the legacy reference is rot. `application/use-cases/spec/test-doubles.ts` lost the `_carryover-08 §9 #5` reference but kept the load-bearing jest-free WHY (the file is included in production builds because `tsconfig.app.json`'s `exclude` only filters `*.spec.ts`). The presentation layer (`orders.controller.ts`), the bootstrap (`main.ts`, `app.module.ts`, all five `index.ts` barrels), all five TypeORM entity files (`Customer`, `Order`, `OrderProduct`, `OrderProductStatus`, `OrderStatus`), the `OrderMapper` / `OrderProductMapper`, the `order-create.model.spec.ts` factory spec, and the three use-case spec files (`create-order` / `confirm-order` / `get-order`) had no narrative comments at baseline and required no edits — confirming the same "wire-format / decorator-driven layers earn no comments" pattern phase-03/phase-04 observed.

### Pending
- phase-06 — api-gateway
- phase-07 — tests-and-scripts

## Established patterns
- **Comments earn their place by carrying WHY**, not WHAT. In particular: leading-class narrations that paraphrase the class name and its type signature ("Framework-free domain entity. Identity is by `id`...") are deleted; comments that carry a non-obvious invariant (e.g. "props must be JSON-stable (no Date / Map / cycles)") are kept and tightened. Phase-01 trimmed these in `libs/ddd/*.ts`.
- **Task-ID / migration-pass references in comments are rot** — `task-04`, `task-09 brief`, `removed in task-14`, "ADR-XYZ records the rationale", and similar pointers were deleted in phase-01. ADR catalogue and git history are the durable records; comments referencing transient task numbers age out fast. Pure ADR-number references that explain a current decision (e.g. "kept in sync with `ROUTING_KEYS`" — the dual-registry policy) are kept because they describe a live constraint, not a past pass.
- **Null/undefined check pattern**: prefer `if (x == null) return ...` over `if (x === undefined || x === null) return ...`. This is the only `==` form the codebase reaches for; everywhere else uses `===`. Applied uniformly in `libs/ddd` (entity-base, value-object-base).
- **Public getters with no production callers are dead surface** even when a spec depends on them — rewrite the spec against the still-public API and drop the getter. Verified in phase-01 by grep across `apps/**`, `libs/**`, and `tests/**` before deletion. Pattern: removing the `domainEvents` getter on `AggregateRoot` collapsed two redundant tests into one without losing meaningful coverage.
- **Hard-constraint frozen surface trumps agent simplification suggestions.** The reuse / quality review agents flagged `BaseTypeormRepository.find/save/softDelete` as potentially dead and `BaseEntity.id` as not-yet-`readonly`; both were correctly left untouched because the phase brief explicitly freezes that public surface. When the simplify review and a hard constraint disagree, the hard constraint wins — note the deferral but make no change.
- **CLAUDE.md-documented public surface behaves like a frozen surface in practice.** Phase-02 found five symbols with zero in-repo callers (`RabbitmqClientFactory`, `CacheHelper`, `@Cacheable`, `MetricsModule`, `TraceContextInterceptor`, and the `ICorrelationPayload` re-export in `libs/observability`) that would normally be deletion candidates per the phase-01 "dead surface" rule. Each is explicitly named in CLAUDE.md as part of its lib's exported surface; the brief makes `CLAUDE.md` out of scope, so deleting them would leave the doc inconsistent. Resolution: keep the symbol, trim any rot in its comment, and record the deletion candidacy under "Open / deferred" so a future pass that updates the docs can close the loop.
- **Placeholder modules / classes are load-bearing imports, not dead code.** `MetricsModule` and `TraceContextInterceptor` look like dead Nest providers but they exist so app modules can already declare the import surface without churning when the real implementation lands. Trim the task-ID rot inside the comment, but keep the placeholder itself.
- **Audit-finding identifiers in comments are preserved as breadcrumbs.** Inline references like `CACHE-010`, `CACHE-011`, `TEST-002`, `TEST-003` follow the same preservation rule as the explicit `AUDIT-2026-05-08` / `AUDIT-2026-05-20` tags called out in the phase brief — they tie a specific mechanism to a specific audit finding, and the link is load-bearing for anyone tracing closure status across `docs/audits/*` and CLAUDE.md's "operational notes" block.
- **Hypothetical future-state postscripts in comments are rot.** Sentences like "The gateway can proxy a `GET /health/notification` to this pattern later" describe code that doesn't exist; they read as planning notes that age into hallucinations. Distinguish from current-state WHY ("RMQ-only service" — true today, structural, explains why `@MessagePattern` is here instead of `@Get`) which is preserved. Pattern verified in `health.controller.ts`.
- **Placeholder adapters keep an ADR-anchored one-line WHY, not a TODO block.** Phase-02 established that placeholder *modules* are load-bearing imports. Phase-03 extends this to placeholder *adapters* (`EmailNotifierAdapter`, `WebhookNotifierAdapter`) — the throwing `send()` is the body; the comment carries the *non-code-visible* WHY ("dependency deliberately not in `package.json`" or "DI slot stays visible alongside the sibling adapter"). The "TODO(post-migration): wire X" form is removed because the throwing body already signals "not implemented" and the wiring sequence belongs in the ADR / a PR description, not in code comments.
- **`*.module.ts` per-module wiring files: keep DI-rebind seam WHY when an ADR documents a rejected alternative.** `notifications.module.ts` retains the "`useExisting` rebind to swap to email/webhook" comment because ADR-011 §3 explicitly rejected the alternative `NotifierService with feature flags` shape — a reviewer seeing `{ provide: NOTIFIER, useExisting: LogNotifierAdapter }` cannot infer from the providers array alone that the feature-flag concrete was considered and rejected. Without a rejected alternative on file, the wiring is self-documenting and the comment is rot. Applied at the per-module `*.module.ts` granularity.
- **Use-case and consumer files were already comment-clean.** Both `SendOrderNotificationUseCase` / `SendLowStockAlertUseCase` and both `OrderEventsConsumer` / `InventoryEventsConsumer` had zero comments at baseline — the class names, decorators (`@EventPattern(ROUTING_KEYS.X)`), and method shapes carry every piece of information a comment would. Treat this as the target shape for the use-case and consumer layers in phases 04–06: if you find yourself writing a comment over a `@MessagePattern` / `@EventPattern` handler that just names the wire pattern, delete it — `ROUTING_KEYS.X` is the doc. Phase-04 confirmed: `StockController` and all four TypeORM entity files matched this target shape with zero edits.
- **Audit-closure breadcrumb blocks compress aggressively when the mechanism lives in an ADR.** Phase-04 trimmed a 12-line "CACHE-001/004 closed by ADR-021 (single-flight in `getOrLoad`, ±10% jitter inside `set`). CACHE-003/009 closed by ADR-022 (schema-version segment + opt-in tenant segment in the key shape). CACHE-005 closed by the `available` flag returned from `get`: when the read fails, `getOrLoad` short-circuits the write-back path so a single Redis-down request produces exactly one warn instead of three." down to a 4-line list. The audit-finding identifiers (`CACHE-001`, `CACHE-004`, `CACHE-005`, etc.) are the load-bearing breadcrumb per the established pattern; the per-closure mechanism narration is rot because the ADR carries the same content with more authority. Keep the **closure mapping** (`X closed by ADR-Y`), drop the **mechanism restatement**.
- **Method-level intentional-no-ops keep a one-line "Intentional X, not Y" framing.** Phase-03 established this for placeholder *adapters* (`EmailNotifierAdapter` body throws). Phase-04 extends it to placeholder *methods*: `StockRabbitmqPublisher.publishStockReserved` returns void with no cross-service consumer, and `StockTypeormRepository.findBySku` returns null because the schema has no SKU column. Both kept a single line of the shape "Intentional null/no-op rather than `not implemented` throw — `<one-clause why>`" because the throwing alternative would force every call site to guard. Drop the future-state narration about why the surface exists at all (that's covered by the port file and/or the ADR).
- **CLAUDE.md-documented external surface absolute-pinning vs internal-surface deletion candidates.** Phase-02 established the "CLAUDE.md-documented public surface behaves like a frozen surface" rule for `libs/**`. Phase-04 found that the analogous rule for `apps/**` is the **audit-document referenced canonical surface**: `infrastructure/cache/spec/stock.cache.spec.ts`'s 9-line canonical-reference header is named in DOCS-001's `informational-no-action` verdict in the 2026-05-20 audit as the canonical convention reference for inventory unit-test layout. Trimming it would invalidate the audit's verdict (the verdict is "we don't need to write the convention in CLAUDE.md because the spec header is the canonical reference"). Distinct rule from the CLAUDE.md case: the cost of removal is not doc-inconsistency, it's audit-record invalidation.
- **Inline interface-method comments are trim candidates by default.** Phase-05 found that `order.repository.port.ts` carried five inline `// Returns X. Used by Y.` comments above method signatures (`findConfirmableOrder`, `customerExists`, `findExistingProductIds`, the `OrderConfirmableProduct` alias, …) — each one paraphrased the method name and parameter types. The interface name + method signature already encode the surface; the inline comments are WHAT-paraphrase. Pattern: delete inline interface-method comments unless they carry a non-obvious WHY (return-shape constraint, performance trade-off, hidden invariant). When a single method *does* carry a non-obvious WHY (e.g. `findOrderResponse` returning joined reference-table fields verbatim because e2e snapshots assert on them), lift it to the file-level header rather than letting it sit inline — file-level WHYs survive signature changes, inline ones decay into stale pointers. Distinct rule from the class-narrating-header pattern (which targets pre-class blocks); this one targets per-method comments inside the interface body.
- **Migration-history spec headers are rot.** Phase-05 deleted the 6-line header on `order.model.spec.ts` that traced the migration from the legacy `OrderConfirmDomain` class ("Migrated from `app/api/order/domain/spec/order-confirm.domain.spec.ts` — the legacy ... folded into `Order.applyInventoryConfirmation`. Assertion shape is preserved verbatim against the new return type..."). The migration is durable in git history; the spec file enforces the assertion shape with its actual `expect(...)` calls; the comment is a planning note that aged into a tour-of-the-past. Distinct from phase-04's `domain/spec/` headers (which were comment-clean at baseline): the migration-history spec header is what *replaces* the class-narrating spec header pattern as the next-most-common spec-file rot.
- **Cross-service contract-test claims belong on the adapter method, not at file-level.** Phase-05 relocated the "TypeScript compile is the cross-service contract test" claim (ADR-013 §7) from a method-body comment on `inventory-confirm.rabbitmq.adapter.ts` to a one-line method header on `reserveOrderStock`. The claim is *non-obvious* (a reader wonders why no runtime contract test exists), so it earns its place — but it sits on the method that *is* the contract test, not floating at file level or inside the method body. Same principle as the interface-method pattern above: hidden-invariant WHYs live where they apply; class-level WHYs go to the class header.

## Files modified by phase
### phase-01
- `libs/common/index.ts` — removed stale "removed in task-14" header comment.
- `libs/contracts/microservices/microservice-message-pattern.enum.ts` — trimmed task-04 migration narrative; preserved the dual-registry rationale (legacy enum + `ROUTING_KEYS`).
- `libs/contracts/retail/events/order-cancelled.event.ts` — stripped the "see task-09 brief" pointer; kept the "reserved, no producer today" WHY.
- `libs/contracts/inventory/inventory.constants.ts` — trimmed the "ADR-012 records the rationale" trailing pointer; kept the cross-service threshold WHY.
- `libs/ddd/entity.base.ts` — removed introductory class-narrating comment; replaced `if (other === undefined || other === null)` with `if (other == null)`.
- `libs/ddd/value-object.base.ts` — same null-check collapse; trimmed the class-narrating header; added a one-line WHY noting that props must be JSON-stable because comparison uses `JSON.stringify`.
- `libs/ddd/aggregate-root.base.ts` — trimmed the class-narrating header; removed the unused `domainEvents` readonly getter (production callers only ever invoke `pullDomainEvents()` / `addDomainEvent`).
- `libs/ddd/domain-event.base.ts` — trimmed the class-narrating header; kept the transport-agnostic WHY.
- `libs/ddd/repository.port.ts` — removed the entire leading comment; the interface name + type parameters are self-documenting.
- `libs/ddd/spec/aggregate-root.base.spec.ts` — merged the two `domainEvents`-getter-dependent tests ("records added events until pulled" + "drains events on pull") into one "buffers added events and drains them on pull" test that asserts via `pullDomainEvents()` only. Test count dropped from 177 → 176, intentionally.
### phase-02
- `libs/messaging/routing-keys.constants.ts` — removed the 4-line migration-history header that paraphrased ADR-008 and referenced the underscored `MicroserviceMessagePatternEnum`. The dual-registry rationale already lives on the contracts side, and the spec test enforces the alignment.
- `libs/messaging/messaging.module.ts` — removed the aggregate-module narration; the `imports` / `exports` pair tells the reader everything the comment used to say.
- `libs/messaging/microservice-client-notification.module.ts` — removed the producer-purpose comment that referenced "task-09" alongside live examples; the queue token and module name already convey the routing.
- `libs/cache/cache.port.ts` — trimmed the 7-line port-contract narrative that ended with an ADR-002 / ADR-006 / ADR-016 / ADR-021 pointer tour, keeping only the "domain code uses the port, only this lib reaches `@nestjs/cache-manager`" boundary WHY.
- `libs/cache/cache-keys.ts` — trimmed the `CacheHelper` "Removed in task-14" line; rewrote the surviving sentence to describe the live "backwards-compat alias surface" intent (the class itself stays because CLAUDE.md documents it).
- `libs/cache/cache.module.ts` — collapsed the 7-line comment about the double `@Global()` mounts down to a 4-line WHY that names both globals (this module's `@Global()` + the underlying Nest `CacheModule`'s `isGlobal: true`) and the reason the second still matters (integration tests resolve `CACHE_MANAGER` directly).
- `libs/cache/redis-cache.adapter.ts` — removed the 6-line class-header migration narrative (task-11 / ADR-016) and slimmed the `inFlight` field comment to keep the rejected-alternative WHY (store-side advisory lock costs two extra Redis round-trips per miss) without the lead-in narration.
- `libs/cache/decorators/cacheable.decorator.ts` — dropped the task-04 / task-11 references in both the decorator header and the `renderKey` positional-rendering comment, keeping the WHY about why named-arg resolution is structurally unavailable inside a decorator.
- `libs/observability/metrics.module.ts` — trimmed "Task-10 lands Prometheus…" to the live "placeholder reserved for Prometheus-format metrics" WHY.
- `libs/observability/trace-context.interceptor.ts` — rewrote the comment from the "Task-10 fills the body" future-tense narrative to the live state ("OTel auto-instrumentation already propagates the active span; this interceptor remains a no-op so app modules can declare the import").
- `libs/observability/http-context.middleware.ts` — trimmed the "preserved verbatim from the libs/common move" migration-history postscript; kept the live "ADR-001 contract" pointer.
### phase-03
- `apps/notification-microservice/src/modules/notifications/infrastructure/notifications.module.ts` — trimmed the leading paraphrase ("Per-module wiring for the notifications bounded context") and kept the DI-rebind seam WHY in tighter form (`useExisting` is the actual provider shape, not the `useClass` the prior comment referenced — corrected at the same time).
- `apps/notification-microservice/src/modules/notifications/presentation/health.controller.ts` — deleted the "gateway can proxy ... later" hypothetical-future postscript; kept the live "RMQ-only service" WHY in one line that names ADR-011 §6.
- `apps/notification-microservice/src/modules/notifications/domain/notification.model.ts` — deleted the 3-line pre-class header that paraphrased the `extends ValueObject<INotificationProps>` shape and the throwing-constructor invariants. The shape encodes the meaning.
- `apps/notification-microservice/src/modules/notifications/application/ports/notifier.port.ts` — initially deleted the pre-interface comment entirely, then restored a single-line boundary WHY ("Use cases depend on this symbol; the concrete transport is selected by `notifications.module.ts`") to match phase-02's `libs/cache/cache.port.ts` precedent for port files.
- `apps/notification-microservice/src/modules/notifications/application/use-cases/spec/test-doubles.ts` — deleted both class-narrating headers (`InMemoryNotifier` capture-purpose, `FakeLogger` "minimal — only invokes assign/info"). Class bodies are 8 and 13 lines; nothing in them is non-obvious.
- `apps/notification-microservice/src/modules/notifications/infrastructure/delivery/log.notifier.adapter.ts` — deleted the 4-line class-narrating header (`@Injectable() class LogNotifierAdapter implements INotifierPort` + a `logger.info(...)` body already convey "Pino log adapter"; the DI-rebind seam description duplicates the comment kept in `notifications.module.ts`).
- `apps/notification-microservice/src/modules/notifications/infrastructure/delivery/email.notifier.adapter.ts` — trimmed the 4-line `TODO(post-migration)` block down to one ADR-anchored sentence carrying the non-obvious WHY: the SMTP dependency is *deliberately* not in `package.json` so the provider choice isn't forced before the business need is settled. Removed the "wire nodemailer" implementation hint (belongs in the future PR, not in code today).
- `apps/notification-microservice/src/modules/notifications/infrastructure/delivery/webhook.notifier.adapter.ts` — same shape as email: trimmed `TODO(post-migration)` block to one ADR-anchored sentence naming the deliberate DI-slot preservation.
### phase-04
- `apps/inventory-microservice/src/modules/stock/domain/stock-item.model.ts` — trimmed the 10-line constructor-invariant restating header to a single-line WHY explaining why `reservedQuantity` defaults to 0 (ledger doesn't track reservations separately); dropped the "future ledger evolutions become invisible" hypothetical-future postscript.
- `apps/inventory-microservice/src/modules/stock/domain/storage.model.ts` — deleted the 5-line class-narrating header (the `extends ValueObject<IStorageProps>` shape + throwing constructor already convey it).
- `apps/inventory-microservice/src/modules/stock/domain/events/stock-low.event.ts` — trimmed the 4-line header to the at-or-below-threshold semantic contract; dropped the publisher-adapter narration (covered by the adapter file).
- `apps/inventory-microservice/src/modules/stock/domain/events/stock-released.event.ts` — trimmed the 3-line header; kept the "no producer wires this today" WHY (otherwise grep gives a false negative).
- `apps/inventory-microservice/src/modules/stock/domain/events/stock-reserved.event.ts` — trimmed the 4-line header to the semantic contract; dropped the in-process-vs-cross-service narration about how the publisher works.
- `apps/inventory-microservice/src/modules/stock/application/ports/stock-cache.port.ts` — trimmed three multi-line ADR-tour blocks (the `tenantId` opt-in narrative, the `IStockCacheGetResult.available` rationale, and the 14-line trailing class-narrating header listing ADR-002 + ADR-021 + ADR-023) down to single-line WHYs anchored on CACHE-005 / ADR-022 / ADR-023. Net: −18 lines in one file.
- `apps/inventory-microservice/src/modules/stock/application/ports/stock-events.publisher.port.ts` — dropped the `_carryover-07 §5 #3` pointer; kept the cold-Observable + broker-ack WHY.
- `apps/inventory-microservice/src/modules/stock/application/ports/stock.repository.port.ts` — trimmed the 5-line port-purpose header to a one-line WHY for the optional `scope` arg.
- `apps/inventory-microservice/src/modules/stock/application/ports/transaction.port.ts` — tightened the `unique symbol` brand WHY by one line; the non-obvious type-system trick (preventing object-literal satisfaction) is preserved.
- `apps/inventory-microservice/src/modules/stock/application/use-cases/add-stock.use-case.ts` — deleted the 5-line "internal-only use case ... kept as its own class so future call sites can depend on the use case" header (hypothetical-future-state + class-narrating).
- `apps/inventory-microservice/src/modules/stock/application/use-cases/get-stock.use-case.ts` — trimmed the 5-line `getOrLoad` ADR-021 explanatory block (the port file's comment already documents the contract); kept the one-line "uncommitted-rows / shared-cache corruption" invariant WHY on the skip-cache branch.
- `apps/inventory-microservice/src/modules/stock/application/use-cases/reserve-stock-for-order.use-case.ts` — trimmed the 4-line ADR-023 lead-in (the port file carries the type-enforced post-commit-ordering WHY) and the 4-line `inventory.stock.low` rationale (replaced with a one-line "publish failures never raise" invariant). **The 13-line `AUDIT-2026-05-08 [CODE-001]` forward-looking-NULL-storage comment block above the `!!item.storageId` filter is preserved verbatim** per the phase brief's hard constraint.
- `apps/inventory-microservice/src/modules/stock/application/use-cases/spec/test-doubles.ts` — deleted three class-narrating headers (`InMemoryStockRepository`, `InMemoryStockCache`, `InMemoryStockEventsPublisher`) mirroring phase-03's `InMemoryNotifier` / `FakeLogger` trim.
- `apps/inventory-microservice/src/modules/stock/application/use-cases/spec/get-stock.use-case.spec.ts` — tightened the 5-line `stockCache` mock comment (kept the ADR-021 breadcrumb anchoring why we mock `getOrLoad` specifically).
- `apps/inventory-microservice/src/modules/stock/application/use-cases/spec/reserve-stock-for-order.use-case.spec.ts` — tightened the 4-line `WithInvalidationMock` header to the ADR-023 mirroring claim.
- `apps/inventory-microservice/src/modules/stock/infrastructure/cache/stock.cache.ts` — compressed the 12-line audit-closure header to a 4-line list (CACHE-001/004 by ADR-021, CACHE-003/009 by ADR-022, CACHE-005 by the `available` flag); tightened the `JITTER_FRACTION` comment, the CACHE-005 outage-skip comment in `getOrLoad`, the inner-leader-race comment, the `configuredTtl` defensive-coerce comment, the `jitterTtl` math comment, the `withInvalidation` privacy-of-prefix-delete comment, and the three-prefixes ADR-022 transition-window comment. All audit-finding identifiers preserved. Net: −33 lines in one file.
- `apps/inventory-microservice/src/modules/stock/infrastructure/messaging/stock-rabbitmq.publisher.ts` — dropped the `_carryover-07 §5 #3` pointer; kept the cold-Observable WHY. Trimmed the `publishStockReserved` no-cross-service-consumer block to a one-line "Intentional no-op rather than `not implemented` throw" framing.
- `apps/inventory-microservice/src/modules/stock/infrastructure/persistence/stock-item.mapper.ts` — deleted the 6-line class-header narrating the aggregate-vs-single-row mapping rationale; lifted the clamp-at-zero WHY onto the class because it applies to both methods; dropped the duplicate per-method clamp comment.
- `apps/inventory-microservice/src/modules/stock/infrastructure/persistence/stock-typeorm.repository.ts` — trimmed the 5-line `findBySku` future-state narration to a one-line "Intentional null, not a 'not implemented' stub" framing matching the publisher's pattern.
- `apps/inventory-microservice/src/modules/stock/infrastructure/persistence/spec/stock-typeorm.repository.spec.ts` — tightened the 4-line `asScope` cast-rationale comment; kept the opaque-type WHY.
- `apps/inventory-microservice/src/modules/stock/infrastructure/stock.module.ts` — trimmed the 4-line per-module wiring header to a one-line `useExisting` rationale (sharing single adapter instance across port-symbol and concrete-class injectors).
### phase-05
- `apps/retail-microservice/src/modules/orders/domain/order.model.ts` — deleted the 5-line `IOrderConfirmationResult` migration narrative ("replaces the legacy `OrderConfirmDomain`..."), the 14-line `Order` class header restating the constructor-throw invariants and the ADR-013 §5 create-path asymmetry, the 3-line `create` factory header and the 2-line `reconstitute` header. Replaced with a one-line WHY on `create` naming the `order_product` no-`quantity`-column invariant, and a one-line WHY on `applyInventoryConfirmation` naming the "subset of reserved ids, not request set" semantic. The `cancel()` header was trimmed to a one-line "no producer wires this today" breadcrumb.
- `apps/retail-microservice/src/modules/orders/domain/order-product.model.ts` — deleted the 3-line class-narrating header (the `extends Entity<number | null>` parameterization conveys the persisted-vs-transient identity claim).
- `apps/retail-microservice/src/modules/orders/domain/customer.model.ts` — trimmed the 5-line "Order owns its customer reference" header to a one-line ADR-013 §3 anchor on the no-Customer-aggregate decision (the rejected alternative is named in ADR-013).
- `apps/retail-microservice/src/modules/orders/domain/order-status.value-object.ts` — deleted the 4-line class-narrating header that mixed enum-narration with the "cancellation is on the recommendation roadmap" hypothetical-future postscript.
- `apps/retail-microservice/src/modules/orders/domain/events/order-created.event.ts` — reframed the header to name the ADR-013 §5 use-case-constructs-the-event invariant (the prior comment incorrectly suggested the event fired "after `Order.create` succeeds" — the aggregate cannot fabricate its own id at create-time, so the use case constructs the event after the repository round-trip).
- `apps/retail-microservice/src/modules/orders/domain/events/order-confirmed.event.ts` — trimmed the 3-line header; kept the "no cross-service subscriber today" breadcrumb (matches phase-04 `stock-released.event.ts`).
- `apps/retail-microservice/src/modules/orders/domain/events/order-cancelled.event.ts` — dropped the `task-09 brief` task-ID rot; kept the "no producer wires this today" + "reserved" breadcrumb.
- `apps/retail-microservice/src/modules/orders/application/ports/inventory-confirm.gateway.port.ts` — trimmed the 9-line port-tour header to a one-line "reply is subset of reserved ids" WHY.
- `apps/retail-microservice/src/modules/orders/application/ports/order-events.publisher.port.ts` — dropped the `_carryover-07 §5 #3 / _carryover-08 §12 #5` carryover-ID pointer rot (matches phase-04 `stock-events.publisher.port.ts`); kept the cold-Observable + broker-ack WHY.
- `apps/retail-microservice/src/modules/orders/application/ports/order.repository.port.ts` — trimmed the 13-line port-purpose header and **five inline method-purpose comments** that paraphrased method signatures (`findConfirmableOrder`, `customerExists`, `findExistingProductIds`, the `OrderConfirmableProduct` alias narration). The single non-obvious WHY (`findOrderResponse` returns joined reference-table fields verbatim because e2e snapshots assert on `name`/`color`) was lifted to the file-level header so it doesn't decay across signature changes. **Net: −20 lines in one file.**
- `apps/retail-microservice/src/modules/orders/application/use-cases/create-order.use-case.ts` — trimmed the 5-line ADR-013 lead-in before `new OrderCreatedEvent(...)`; replaced the 2-line catch-block comment with a one-line "publish failures never raise" invariant (the order is already persisted).
- `apps/retail-microservice/src/modules/orders/application/use-cases/confirm-order.use-case.ts` — deleted the 3-line "the existing legacy path did the same" migration-history rot on the inventory-RPC catch block; trimmed the 6-line drain-pattern + "publisher port surface is wired" hypothetical-future block before `pullDomainEvents()` to a one-line invariant on the inner try block.
- `apps/retail-microservice/src/modules/orders/application/use-cases/get-order.use-case.ts` — deleted the 4-line `IOrderHeaderView` interface header with its "today the gateway only needs..." hypothetical-future implication; the interface field is self-documenting.
- `apps/retail-microservice/src/modules/orders/application/use-cases/spec/test-doubles.ts` — dropped the `_carryover-08 §9 #5` reference; kept the load-bearing jest-free WHY (production builds include the file because `tsconfig.app.json`'s `exclude` only filters `*.spec.ts`). Also deleted the 4-line `buildPersistedOrder` factory-narrating header and the 3-line `save` method-narrating block.
- `apps/retail-microservice/src/modules/orders/domain/spec/order.model.spec.ts` — deleted the 6-line migration-history spec header ("Migrated from `app/api/order/domain/spec/order-confirm.domain.spec.ts`..."); the spec's actual `expect(...)` calls enforce the assertion shape against the new return type.
- `apps/retail-microservice/src/modules/orders/infrastructure/messaging/inventory-confirm.rabbitmq.adapter.ts` — relocated the "TypeScript compile is the cross-service contract test" claim (ADR-013 §7) from a 4-line method-body comment to a 3-line method header on `reserveOrderStock`. The claim is non-obvious (a reader wonders why no runtime contract test exists) and now sits on the method that *is* the contract test.
- `apps/retail-microservice/src/modules/orders/infrastructure/messaging/order-rabbitmq.publisher.ts` — dropped the `_carryover-07 §5 #3` and `_carryover-08 §9 #4` carryover-ID pointer rot; kept the cold-Observable WHY and the empty-string defensive-fallback WHY on the `correlationId ?? ''` line.
- `apps/retail-microservice/src/modules/orders/infrastructure/orders.module.ts` — trimmed the 3-line per-module wiring header to a one-line `useExisting` rationale (matches phase-04 `stock.module.ts`).
- `apps/retail-microservice/src/modules/orders/infrastructure/persistence/customer.mapper.ts` — deleted the 4-line class header (the VO-vs-aggregate discussion already lives on `customer.model.ts`; the mapper is a 3-line static `toDomain`).
- `apps/retail-microservice/src/modules/orders/infrastructure/persistence/order-typeorm.repository.ts` — tightened the cross-service-`product`-table WHY by one line; the load-bearing claim (retail reads from `product` for the order-create existence check, owned by inventory) was preserved with the ADR-013 anchor.
- `apps/retail-microservice/src/modules/orders/infrastructure/persistence/spec/order.mapper.spec.ts` — deleted the 4-line round-trip-purpose spec header (matches phase-04's "spec class-narrating headers are deleted" rule).
- `apps/retail-microservice/src/modules/orders/presentation/pipes/order-create.pipe.ts` — deleted the 5-line class-narrating header about presentation-vs-application + the boundary-rule narration; the `@Injectable()` + `IOrderRepositoryPort` injection + `RpcException` throws are self-documenting.
- `apps/retail-microservice/src/modules/orders/presentation/pipes/order-confirm.pipe.ts` — deleted the 4-line class-narrating header counterpart.
### phase-06
_(initially empty)_
### phase-07
_(initially empty)_

## Test status checkpoints
### phase-01
- `yarn lint` — exit 0, no output (clean).
- `yarn test:unit` — 30 suites passed, 176 tests passed (down from 177 baseline due to the spec merge described above; no test failures). One Jest worker did not exit gracefully — pre-existing condition observed at baseline, unrelated to phase-01 changes.
- E2E — not run (out of scope per phase brief: "These libs are pure types / Joi schema / TypeORM base classes; they have no wire-format or DB-state surface that e2e would exercise distinctly").
### phase-02
- `yarn lint` — exit 0, no output (clean).
- `yarn test:unit` — 30 suites passed, 176 tests passed (matches phase-01 final count). The pre-existing Jest worker-not-exiting warning still appears (`A worker process has failed to exit gracefully…`), same as baseline — not introduced by this phase.
- `yarn test:e2e` — full reload (`test:infra:reload` → migrations → seed → tests). 3 suites passed, 35 tests passed, 42 snapshots passed. Force-exit warning unchanged from baseline.
### phase-03
- `yarn lint` — exit 0, no output (clean).
- `yarn test:unit` — 30 suites passed, 176 tests passed (matches phase-02 final). Same pre-existing Jest worker-not-exiting warning ("A worker process has failed to exit gracefully…") as the baseline and phase-02 — not introduced by this phase.
- `yarn test:e2e` — full reload (`test:infra:reload` → migrations → seed → tests) per the phase brief's requirement for any phase that touches `@EventPattern` consumers / the RMQ wire interface. 3 suites passed (`system-api.e2e-spec.ts`, `auth.e2e-spec.ts`, `notification.e2e-spec.ts`), 35 tests passed, 42 snapshots passed. Force-exit warning unchanged from baseline. The `notification.e2e-spec.ts` smoke test — which exercises the full consumer → use-case → notifier path by publishing a synthetic `retail.order.created` event directly to the queue — passes, confirming the comment-only trims did not perturb the consumer / notifier behavior.
### phase-04
- `yarn lint` — exit 0, no output (clean).
- `yarn test:unit` — 30 suites passed, 176 tests passed (matches phase-03 final). Same pre-existing Jest worker-not-exiting warning ("A worker process has failed to exit gracefully…") as the baseline through phase-03 — not introduced by this phase.
- `yarn test:e2e` — full reload (`test:infra:reload` → migrations → seed → tests) per the phase brief's requirement for any phase that touches the cache adapter and `@MessagePattern` handler surroundings. 3 suites passed (`system-api.e2e-spec.ts`, `auth.e2e-spec.ts`, `notification.e2e-spec.ts`), 35 tests passed, 42 snapshots passed. Force-exit warning unchanged from baseline. The `system-api.e2e-spec.ts` suite exercises the full `inventory.product-stock.get` (cache-aside read path through `GetStockUseCase` → `StockCache.getOrLoad` → `StockTypeormRepository.aggregateForProduct`) and `inventory.order.confirm` (transactional reserve through `ReserveStockForOrderUseCase` → `withInvalidation` → ADR-022 three-prefix transition fan-out) flows, confirming the comment-only trims did not perturb the cache adapter, the transactional reserve path, or the cross-service `inventory.stock.low` emission to the notification microservice.
### phase-05
- `yarn lint` — exit 0, no output (clean).
- `yarn test:unit` — 30 suites passed, 176 tests passed (matches phase-04 final). Same pre-existing Jest worker-not-exiting warning ("A worker process has failed to exit gracefully…") as the baseline through phase-04 — not introduced by this phase.
- `yarn test:e2e` — full reload (`test:infra:reload` → migrations → seed → tests) per the phase brief's requirement for any phase that touches `@MessagePattern` handlers, the cross-service confirm RPC adapter, and event publishers. 3 suites passed (`system-api.e2e-spec.ts`, `auth.e2e-spec.ts`, `notification.e2e-spec.ts`), 35 tests passed, 42 snapshots passed. Force-exit warning unchanged from baseline. The `system-api.e2e-spec.ts` suite exercises the full `retail.order.create` (gateway → `OrderCreatePipe` existence checks → `CreateOrderUseCase` → `retail.order.created` to notification) and `retail.order.confirm` (gateway → `OrderConfirmPipe` line-item load → `ConfirmOrderUseCase` → `INVENTORY_CONFIRM_GATEWAY` → inventory `withInvalidation` → `retail.order.confirmed`) flows, confirming the comment-only trims did not perturb the cross-service confirm seam, the event-publisher fan-out, or the pre-RPC pipe validation.
### phase-06
_(initially empty)_
### phase-07
_(initially empty)_

## Open / deferred
- `ValueObject#equals` (`libs/ddd/value-object.base.ts`) uses `JSON.stringify` deep-compare, which is order-sensitive, drops `undefined`, lossy on `Date`/`BigInt`/`Map`, and throws on cycles. Phase-01 added an inline WHY noting the JSON-stable-props invariant rather than swap the algorithm — a structural rewrite risks behavior-changing existing VOs that may pass nested objects or `Date` props. If a future phase introduces a VO with non-JSON-stable props (or a Subagent reports a correctness bug in equality), revisit and consider a shallow `Object.keys` + `===` loop (or a curated deep-equal helper that stays framework-free).
- `BaseTypeormRepository.find / save / softDelete` (`libs/database/base-typeorm.repository.ts`) appear unused by the only current subclass (`StockTypeormRepository`, which constructs its own `productStockRepository` and bypasses the base). The Hard Constraint freezes the public surface, so phase-01 left them. If a future phase relaxes the constraint (or audits and confirms zero downstream callers across all aggregates), these methods could be removed to unblock further refactors — but the abstract `toDomain` / `toEntity` hooks may still be load-bearing for other adapters that have not yet adopted the base.
- `BaseEntity.id` (`libs/database/base.entity.ts`) is mutable. A `readonly` flip would prevent accidental PK overwrites at the domain level; TypeORM's `PrimaryGeneratedColumn` should tolerate it (assignment is via `Object.assign` during hydration). Not done in phase-01 because it's a public-surface change on a frozen file; flag for an explicit follow-up if entity hydration is verified safe.
- `IDomainEventEnvelope` consolidation — four event interfaces (`IRetailOrderCreatedEvent`, `IRetailOrderConfirmedEvent`, `IRetailOrderCancelledEvent`, `IInventoryStockLowEvent`) each redeclare `occurredAt: string` on top of `ICorrelationPayload`. The reuse-review agent suggested extracting a shared `IDomainEventEnvelope extends ICorrelationPayload { occurredAt: string }`. Phase-01 skipped it: ABI-frozen field names mean the indirection saves only ~4 lines while adding a new contracts file, and inline declarations are more self-contained for wire-format reading. Worth revisiting only if a fifth event with the same envelope ships.
- `RabbitmqClientFactory` (`libs/messaging/rabbitmq.client.factory.ts`) has zero in-repo callers (`grep` across apps/libs/tests). The factory was introduced for "one-off proxies in tests / bootstrap scripts that don't want a Nest module" but no such caller actually exists. CLAUDE.md documents it as part of the `@retail-inventory-system/messaging` surface, and `CLAUDE.md` is out of scope for this phase. Worth deleting in a future pass that also trims the doc bullet.
- `CacheHelper` (`libs/cache/cache-keys.ts`) is documented as a backwards-compat alias for the legacy `productStock*` key shape but has zero in-repo callers. CLAUDE.md mentions it under `@retail-inventory-system/cache`. Deletion candidate alongside the CLAUDE.md edit.
- `@Cacheable` decorator (`libs/cache/decorators/cacheable.decorator.ts`) has zero in-repo call sites — every aggregate cache uses the explicit `ICachePort` adapter pattern (see `StockCache.withInvalidation` etc.) rather than the decorator. CLAUDE.md documents it as part of the cache lib's surface. Decision options: (a) keep both decorator and doc, (b) delete decorator + remove the bullet from CLAUDE.md.
- `MetricsModule` (`libs/observability/metrics.module.ts`) is an empty Nest module placeholder reserved for Prometheus metrics. Zero callers today. CLAUDE.md describes it as a placeholder. Same defer-pattern as the above; keep until a future phase lands real metrics or removes the surface.
- `TraceContextInterceptor` (`libs/observability/trace-context.interceptor.ts`) is a passthrough placeholder; OTel auto-instrumentation already covers cross-service span propagation, so the interceptor is genuinely no-op. Zero callers. Defer for the same CLAUDE.md reason.
- `ICorrelationPayload` re-export in `libs/observability/correlation.types.ts` exists "for app-side consumers that otherwise have no reason to import from `@retail-inventory-system/contracts`" — but app-side `grep` finds no such consumer. The re-export is currently dead. CLAUDE.md lists it as part of the observability surface. Defer.
- `LoggerModuleConfig.customProps` (`libs/observability/logger.module.ts`) wraps a single fixed object inside a closure (`customProps: (): { app: AppNameEnum } => customProps`). The closure round-trip is purely stylistic; a `customProps: () => ({ app: appName })` arrow is one line shorter and equivalent. Skipped this phase because the file is dense with load-bearing behavior (trace correlation hook, E2E destination hook, CI-regression `LOG_LEVEL` override) and a stylistic-only change isn't worth the audit cost. Revisit if the file is touched for substantive reasons.
- Hoisting `trace.getTracer(TRACER_NAME)` in `libs/cache/redis-cache.adapter.ts` — every public method calls `trace.getTracer(TRACER_NAME)` on entry. The OTel API caches tracer lookups internally, so a single `private readonly tracer = trace.getTracer(TRACER_NAME)` is semantically identical and 5 call sites collapse to 1. Skipped this phase: the cost of being wrong on initialization-order semantics (tracer-provider not yet installed at construction) is higher than the line-count win. Revisit when the OTel SDK version is bumped or other adapter changes land.
- `FakeLogger.assign` + the `assignments: Record<string, unknown>[]` array on the class (`apps/notification-microservice/src/modules/notifications/application/use-cases/spec/test-doubles.ts`) are not invoked or asserted by any spec — both use cases (`SendOrderNotificationUseCase`, `SendLowStockAlertUseCase`) pass `correlationId` inline on `logger.info()` per ADR-011 §7, never via `assign()`. Phase-03 left the surface in place because: (1) ADR-011 §7 explicitly distinguishes "inside an `@EventPattern` / `@MessagePattern` handler, pass `correlationId` as a log field; inside an HTTP controller path, `assign()` is fine" — so `assign` is a deliberate seam for the future case where an HTTP-bound use case lands here; (2) the phase-03 brief's DOCS-001 reference documents the class-based / hand-rolled spec style as meaningful design, and trimming surface that mirrors `PinoLogger`'s shape risks slow-walking the style toward the inventory/retail `jest.Mocked<Pick<...>>` pattern the constraint forbids. Deletion candidate only if a future audit confirms no HTTP path will ever ride through these tests.
- `INotificationHealthResponse` interface in `apps/notification-microservice/src/modules/notifications/presentation/health.controller.ts` is exported but has zero in-repo importers (verified via `grep -r "INotificationHealthResponse" apps/ libs/ tests/ test/`). Could be inlined to the `ping()` return type. Skipped this phase because explicit `I*`-prefixed interfaces are a project-wide convention (CLAUDE.md / ESLint rule), and inlining the type would make the controller's return shape less greppable for any future cross-service consumer. Defer; revisit only if a broader pass removes other no-importer exported interfaces in the gateway / inventory presentation layers.
- `OrderConfirmableProduct` type alias in `apps/retail-microservice/src/modules/orders/application/ports/order.repository.port.ts` (`export type OrderConfirmableProduct = IOrderProductConfirm`) has zero in-repo callers (`grep` across `apps/`, `libs/`, `tests/`, `test/`). The alias was originally retained "so pipes/use cases don't need to import the contracts package twice", but every caller imports `IOrderProductConfirm` directly. Phase-05 trimmed the alias's narrative comment but left the symbol because the alias is part of the port-file public surface and inventory's parallel `IStockCacheGetResult` was kept under the same rule. Deletion candidate: drop the export and remove the `IOrderProductConfirm` import if it's not otherwise used in the file.
- `IOrderHeaderView` interface in `apps/retail-microservice/src/modules/orders/application/use-cases/get-order.use-case.ts` is exported but only used locally as the return-type annotation on `findHeaderById`. No caller imports it (verified via `grep`). Same defer-pattern as `INotificationHealthResponse`: explicit `I*`-prefixed interfaces are a project-wide convention; inlining `{ statusId: OrderStatusEnum }` would make the return shape less greppable. Defer; if a broader pass removes other no-importer exported interfaces, this one closes alongside.
- `IOrderEventsPublisherPort.publishOrderCancelled` (and the matching `OrderRabbitmqPublisher.publishOrderCancelled` adapter method) has **no production caller** — `confirm-order.use-case.ts` only drains `OrderConfirmedEvent` from `pullDomainEvents()`, and `Order.cancel()` itself has no caller. The port-side and adapter-side methods exist only because the `OrderCancelledEvent` is reserved port surface (ADR-013 §4 explicitly: "The aggregate's `cancel()` method and the publisher port surface exist so a future cancel flow plugs in without re-shaping the module"). Same pattern as phase-04's `StockRabbitmqPublisher.publishStockReserved` — kept as reserved port surface, comment trimmed of rot. Deletion candidate only if a future ADR removes the cancel-flow reservation.
- `IOrderEventsPublisherPort.publishOrderConfirmed` and its adapter have **no cross-service consumer** — `retail.order.confirmed` is emitted today but no notification-side `@EventPattern` subscribes to it. ADR-013 §6 explicitly names this: "Reserved for future cross-service consumers; no subscriber today." Phase-05 kept the producer wired (the confirm use case publishes, and the publish-failure-warn-logged invariant is tested in `confirm-order.use-case.spec.ts`) because the wire-format contract `IRetailOrderConfirmedEvent` is part of `libs/contracts/retail/events/` and the producer side is a non-trivial integration test surface. No deletion candidate; recording only so a future pass that audits cross-service event subscribers has the breadcrumb.
