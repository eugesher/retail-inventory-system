// `ICorrelationPayload` is a cross-service payload contract — its canonical
// home is `libs/contracts`. Re-exported here for app-side consumers that
// otherwise have no reason to import from `@retail-inventory-system/contracts`.
export { ICorrelationPayload } from '@retail-inventory-system/contracts';
